# Frontend talks to the API via the relative /api path (matches the nginx
# reverse proxy in production). Leave VITE_API_BASE_URL unset to use it.
# VITE_API_BASE_URL=/api

# In `npm run dev`, Vite proxies /api to this target (the FastAPI backend).
VITE_DEV_API_TARGET=http://localhost:8000
